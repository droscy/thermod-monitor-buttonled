#!/usr/bin/make -f

#export DH_VERBOSE=1
export PYBUILD_NAME=thermod-monitor-buttonled

%:
	dh $@ --with python3,systemd --buildsystem=pybuild

override_dh_installinit:
	dh_installinit --noscripts

override_dh_systemd_enable:
	dh_systemd_enable thermod-monitor-buttonled.service
